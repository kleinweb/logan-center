query SiteMenus {
  # headerMenu: menu(id: "PRIMARY", idType: LOCATION) {
  #   ...MenuItems
  # }
  headerMenu: menuItems(where: {location: PRIMARY_MENU}) {
    nodes {
      key: id
      parentId
      title: label
      url
    }
  }
}
