import {ChevronRight} from 'lucide-react'

/**
 * Icon component of the type exported by the `lucide-react` library.
 *
 * Note that the use of {@link ChevronRight} here is arbitrary.
 */
export type LucideIconComponent = typeof ChevronRight
